API
===

The orientation module does not have a documented Python API yet — it is
currently used through the graphical user interface.

The underlying method is provided by the CompactionAnalyzer package, which can
be scripted directly: https://github.com/davidbhr/CompactionAnalyzer

For the Python API of the 3D TFM solver, see :doc:`../3d_tfm/api`.
